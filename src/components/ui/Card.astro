---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-2xl border border-(--border) bg-(--surface) p-6 ${className}`}
>
  <slot />
</div>
